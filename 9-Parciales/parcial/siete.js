function Mostrar()
{
    var notas;
    var sexo
    var contador=0;
    while (contador<3);
    {
        notas=prompt ("ingrese notas")
        contador++
        
        while (notas<0 || notas>10)
        {
            notas=parseInt (prompt ("solo numeros del 0 a 10"));
        }
        while (sexo !="f"&& sexo !="m")
        {
            
        }
}   

}
